---
description: TCF 刷题项目上下文与规范 — 新对话先读再动手
alwaysApply: true
---

# TCF-Training 项目规则

- **先读再写**：动手改代码前，先读项目根目录的 `PROJECT-CONTEXT.md`。若涉及题目结构或校验，再读 `docs/tcf-question-schema.md` 和 `docs/claude-project-instruction-v2.md`（若 docs 被 gitignore，用户可临时取消忽略或把 schema 放在根目录）。
- **题目与校验**：类型和校验逻辑以 `docs/tcf-question-schema.md` 为唯一真相。CO/CE 有 question、options、correctAnswer；EE/EO **没有**这三项。校验器在 `src/lib/question-engine/validator.ts`，按 `module` 分支。
- **UI 与文案**：界面文案用中文；错误用红色、警告用黄色区分；导入页需支持粘贴 JSON、解析、按题型预览、补全缺失数据、加载示例题。
